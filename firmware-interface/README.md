# Firmware Interface

This directory documents host-visible firmware interfaces for OSF GREEN.

The Linux RFC currently relies on OSF0 capability, device status, and sensor
sample frames over UART. Other firmware demo interfaces should be documented
separately and not treated as Linux ABI.
